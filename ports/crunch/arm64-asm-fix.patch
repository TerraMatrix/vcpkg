--- a/crnlib/crn_platform.h
+++ b/crnlib/crn_platform.h
@@ -39,7 +39,11 @@
 #if CRNLIB_USE_WIN32_API
    #define CRNLIB_BREAKPOINT DebugBreak();
    #define CRNLIB_BUILTIN_EXPECT(c, v) c
 #elif defined(__GNUC__)
-   #define CRNLIB_BREAKPOINT asm("int $3");
+   #if defined(__aarch64__) || defined(_M_ARM64)
+      #define CRNLIB_BREAKPOINT asm("brk #0");
+   #else
+      #define CRNLIB_BREAKPOINT asm("int $3");
+   #endif
    #define CRNLIB_BUILTIN_EXPECT(c, v) __builtin_expect(c, v)
 #else
    #define CRNLIB_BREAKPOINT
--- a/crnlib/crn_atomics.h
+++ b/crnlib/crn_atomics.h
@@ -13,7 +13,11 @@
 #if defined(__GNUC__) && CRNLIB_PLATFORM_PC
 extern __inline__ __attribute__((__always_inline__,__gnu_inline__)) void crnlib_yield_processor()
 {
+   #if defined(__aarch64__) || defined(_M_ARM64)
+      __asm__ __volatile__("yield");
+   #else
    __asm__ __volatile__("pause");
+   #endif
 }
 #else
 CRNLIB_FORCE_INLINE void crnlib_yield_processor()
--- a/crnlib/crn_core.h
+++ b/crnlib/crn_core.h
@@ -34,7 +34,7 @@
    #define CRNLIB_PLATFORM_PC 1

-   #if defined(_WIN64) || defined(__MINGW64__) || defined(_LP64) || defined(__LP64__)
+   #if defined(_WIN64) || defined(__MINGW64__) || defined(_LP64) || defined(__LP64__) || defined(_M_ARM64) || defined(_M_ARM64EC)
       #define CRNLIB_PLATFORM_PC_X64 1
       #define CRNLIB_64BIT_POINTERS 1
       #define CRNLIB_CPU_HAS_64BIT_REGISTERS 1
@@ -64,7 +64,7 @@
    // GCC x86 or x64, pthreads for threading and GCC built-ins for atomic ops.
    #define CRNLIB_PLATFORM_PC 1

-   #if defined(_WIN64) || defined(__MINGW64__) || defined(_LP64) || defined(__LP64__)
+   #if defined(_WIN64) || defined(__MINGW64__) || defined(_LP64) || defined(__LP64__) || defined(__aarch64__) || defined(_M_ARM64)
       #define CRNLIB_PLATFORM_PC_X64 1
       #define CRNLIB_64BIT_POINTERS 1
       #define CRNLIB_CPU_HAS_64BIT_REGISTERS 1
--- a/crnlib/crn_miniz.h
+++ b/crnlib/crn_miniz.h
@@ -172,7 +172,7 @@
 //#define MINIZ_NO_MALLOC

-#if defined(_M_IX86) || defined(_M_X64) || defined(__i386__) || defined(__i386) || defined(__i486__) || defined(__i486) || defined(i386) || defined(__ia64__) || defined(__x86_64__)
+#if defined(_M_IX86) || defined(_M_X64) || defined(__i386__) || defined(__i386) || defined(__i486__) || defined(__i486) || defined(i386) || defined(__ia64__) || defined(__x86_64__) || defined(__aarch64__) || defined(_M_ARM64)
 // MINIZ_X86_OR_X64_CPU is only used to help set the below macros.
 #define MINIZ_X86_OR_X64_CPU 1
 #endif
@@ -182,7 +182,7 @@
 #if MINIZ_X86_OR_X64_CPU
 // Set MINIZ_USE_UNALIGNED_LOADS_AND_STORES to 1 on CPU's that permit efficient integer loads and stores from unaligned addresses.
 #define MINIZ_USE_UNALIGNED_LOADS_AND_STORES 1
-#endif
+#endif

-#if defined(_M_X64) || defined(_WIN64) || defined(__MINGW64__) || defined(_LP64) || defined(__LP64__) || defined(__ia64__) || defined(__x86_64__)
+#if defined(_M_X64) || defined(_WIN64) || defined(__MINGW64__) || defined(_LP64) || defined(__LP64__) || defined(__ia64__) || defined(__x86_64__) || defined(__aarch64__) || defined(_M_ARM64)
 // Set MINIZ_HAS_64BIT_REGISTERS to 1 if operations on 64-bit integers are reasonably fast (and don't involve compiler generated calls to helper functions).
 #define MINIZ_HAS_64BIT_REGISTERS 1
 #endif
--- a/crnlib/crn_hash.cpp
+++ b/crnlib/crn_hash.cpp
@@ -7,7 +7,7 @@

 #undef get16bits
 #if (defined(__GNUC__) && defined(__i386__)) || defined(__WATCOMC__) \
-   || defined(_MSC_VER) || defined (__BORLANDC__) || defined (__TURBOC__)
+   || defined(_MSC_VER) || defined (__BORLANDC__) || defined (__TURBOC__) \
+   && !defined(__aarch64__) && !defined(_M_ARM64)
 #define get16bits(d) (*((const uint16 *) (d)))
 #endif

 #if !defined (get16bits)